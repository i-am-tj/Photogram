package com.example.photogrampostservice.controllers;

/*
    Post Management:
    POST /api/posts: Create a new post.
    GET /api/posts/{postId}: Get details of a specific post.
    PUT /api/posts/{postId}: Update an existing post.
    DELETE /api/posts/{postId}: Delete a post.

    Post Interaction:
    POST /api/posts/{postId}/like: Like a post.
    POST /api/posts/{postId}/comment: Add a comment to a post.
    POST /api/posts/{postId}/share: Share a post.
    DELETE /api/posts/{postId}/like: Unlike a post.
    DELETE /api/posts/{postId}/comment/{commentId}: Delete a comment from a post.

    Post Search: // P1
    GET /api/posts/search?query={query}: Search for posts based on a query string.
    GET /api/posts/trending: Get trending posts.

 */
public class PostController {
}
